function estiloBoton(borde, colorBorde, colorRelleno) {
  strokeWeight(borde);
  stroke(colorBorde);
  fill(colorRelleno);  
}

function estiloTexto(tamañoTxt) {
 noStroke();
 fill(255);
 textSize(tamañoTxt);
}
