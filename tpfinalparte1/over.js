//hover boton inicio
function hover1(areaX1, areaX2, areaY1, areaY2, colorHover, colorNormal, strokeHover, strokeNormal) {
  if (mouseX > areaX1 && mouseX < areaX2 && mouseY > areaY1 && mouseY < areaY2) {
    color1A = colorHover;
    stroke1A = strokeHover;
    console.log("hover", color1A,color1B);
  } else {
    color1A = colorNormal
    stroke1A = strokeNormal
  }
}
//hover boton creditos
function hover2(areaX1, areaX2, areaY1, areaY2, colorHover, colorNormal, strokeHover, strokeNormal) {
  if (mouseX > areaX1 && mouseX < areaX2 && mouseY > areaY1 && mouseY < areaY2) {
    color2A = colorHover;
    stroke2A = strokeHover;
    console.log("hover", color1A,color1B);
  } else {
    color2A = colorNormal
    stroke2A = strokeNormal
  }
}
//hover boton reiniciar
function hover3(areaX1, areaX2, areaY1, areaY2, colorHover, colorNormal, strokeHover, strokeNormal) {
  if (mouseX > areaX1 && mouseX < areaX2 && mouseY > areaY1 && mouseY < areaY2) {
    color3A = colorHover;
    stroke3A = strokeHover;
    console.log("hover", color1A,color1B);
  } else {
    color3A = colorNormal
    stroke3A = strokeNormal
  }
}
//hover boton siguiente
function hover4(areaX1, areaX2, areaY1, areaY2, colorHover, colorNormal, strokeHover, strokeNormal) {
  if (mouseX > areaX1 && mouseX < areaX2 && mouseY > areaY1 && mouseY < areaY2) {
    color4A = colorHover;
    stroke4A = strokeHover;
    console.log("hover", color1A,color1B);
  } else {
    color4A = colorNormal
    stroke4A = strokeNormal
  }
}
